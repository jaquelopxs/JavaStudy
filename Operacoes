String[] buttons = {
        "7", "8", "9", "/",
        "4", "5", "6", "*",
        "1", "2", "3", "-",
        "C", "0", "=", "+", 
        "^", "√"
};

else if (comando.equals("^")) { // Exponenciação
    if (!display.getText().isEmpty()) {
        num1 = Double.parseDouble(display.getText());
        operador = "^";
        novoNumero = true;
    }
} else if (comando.equals("√")) { // Raiz quadrada
    if (!display.getText().isEmpty()) {
        num1 = Double.parseDouble(display.getText());
        resultado = Math.sqrt(num1);
        display.setText(new DecimalFormat("0.######").format(resultado));
        novoNumero = true;
    }
}
